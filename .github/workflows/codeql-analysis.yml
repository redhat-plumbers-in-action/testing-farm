---

name: CodeQL

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '31 7 * * 3'

permissions:
  contents: read

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        language: [ TypeScript ]

    permissions:
      actions: read
      security-events: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@f095bcc56b7c2baf48f3ac70d6d6782f4f553222

      - name: Initialize CodeQL
        uses: github/codeql-action/init@fff3a80b5bbdd29f1453bf4554afabb83609be14
        with:
          languages: ${{ matrix.language }}
          source-root: src

      - name: Autobuild
        uses: github/codeql-action/autobuild@fff3a80b5bbdd29f1453bf4554afabb83609be14

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@fff3a80b5bbdd29f1453bf4554afabb83609be14

...
